{% extends "UrbikUserBundle::layout.html.twig" %}

{% block body %}
<h2 class="text-center">Modification inscrit</h2>
<div class="container">
    <div class="well">
        {{ form(form) }}
        <a href="{{path('urbik_user_admin_view', {'id': user.id }) }}" class="btn btn-primary">Annuler</a>
    </div>
</div>
{% endblock %}